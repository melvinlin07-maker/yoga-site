# CLAUDE.md — 鑫彥瑜珈網站 工作規則

本檔案是之後所有 Claude Code 對話在這個 repo 內必須遵守的規則。與 `PROJECT.md`、`DECISIONS.md` 搭配使用。

---

## 開工前

**每次開工前,必須先讀:**
1. `PROJECT.md` — 現況事實與系統地圖
2. `DECISIONS.md` — 已做過的決策與原因
3. `CLAUDE.md`(本檔案)— 工作規則

不要依賴對話記憶推論現況,一律以這三份文件 + 實際 `git status` / 檔案內容為準。若文件內容與實際檔案不一致,以實際檔案為準,並回報給使用者更新文件。

---

## 範圍紀律

- **不擴大修改範圍。** 使用者交辦的任務是什麼,就只做什麼。發現其他問題(過時檔案、命名不一致、可優化項目)先記錄下來回報,不要順手一起改。
- 一個任務對應一個明確目的,不做「順便重構」「順便清理」。

---

## 高風險檔案

以下檔案改一次會影響很多頁面,**修改前必須先向使用者報告影響範圍(哪些頁面、哪些行為會變動),取得確認後才動手:**

- `site.js`(全站 LINE 按鈕邏輯、GA4、Meta Pixel、行動選單)
- `theme.css`(全站視覺樣式)
- `sitemap.xml` / `robots.txt`(全站 SEO 索引範圍)

---

## 部署紀律

- **不自行 commit。**
- **不自行 push。**
- **不自行 deploy。**

除非使用者在當次對話中明確要求執行,且僅限該次授權範圍,不可推及未來對話或其他檔案。

---

## 驗證要求

修改網站內容(HTML/CSS/JS)後,必須提供驗證證據,例如:
- 瀏覽器截圖
- 實際開啟頁面確認渲染結果
- 若牽涉 LINE CTA、表單送出等互動行為,說明是否已實測,或明確標註「尚未測試」

不得僅憑「程式碼看起來正確」就回報任務完成。

---

## 版本用語規範

**檔名版本 ≠ 商業測試版本。**

- 檔名版本(如 `-v1` `-v2` `-v3`)= 技術 / 檔案版本,反映建立順序,不代表商業測試分組
- 商業版本(例如 `data-line-message` 內文字標記「V1」「V3」)= A/B 測試版本標記,由使用者刻意指定,可能與檔名版本不同(例:`neck-release-v3.html` 檔名是 v3,但商業標記是「V1」,這是刻意區分,非錯誤)

**不得自行用檔名推論商業版本,提及版本時必須註明是指哪一種。**

---

## Capability Discovery(盲點掃描)

- 目標不是找越多功能越好,而是找「系統已具備但使用者可能不知道、且有高商業價值的能力」
- **優先找原生 / 免費能力**,避免因為技術上能做就推薦
- **避免 overengineering**:不因為某功能技術上可行就建議實作,需評估商業價值、實作成本、風險、是否需要付費
- **每次最多提出 3 個高價值盲點**,每個需說明:能解決什麼問題、商業價值、實作成本、風險、是否付費、是否值得現在做
- 不得替使用者做商業決定,只能提供選項與判斷依據

---

## 商業決策界線

- 不自行替使用者決定定價、Offer 內容、廣告版本去留、頁面上下線
- 遇到需要商業判斷的節點(例如某測試頁是否該下線),列為待確認事項回報,不擅自執行
